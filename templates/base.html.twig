<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer">
        {% block stylesheets %}
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') |raw }}{% endblock %}
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO" crossorigin="anonymous"></script>
        {% endblock %}
    </head>
    <body>
        <header class="width100">
            <nav class="backgroundPink width100 flex alignCenter">
                {# je met le / devant pour forcer l'image à être récupérer à la racine car sinon il cherche juste dansle doosier avant #}
                {# si je suis dans la page compte/2 il cherchera l'image dans compte/img/plumeida.png #}
                <a href="/.." class="logoSite backgroundPink"> <img src="/img/plumedia.png" alt="Plumedia logo" class="logoSite"></a>
                <a href="{{path('app_category')}}" class="linkBtn textBlack backgroundPink">Catégories</a>
                <a href="/.." class="linkBtn textBlack backgroundPink">Calendrier</a>
                <a href="{{path('populars')}}" class="linkBtn textBlack backgroundPink">Populaires</a>
                {# si l'utilisateur est connecter alors je permet directement la déconnexion et l'accès à son compte  #}
                    {% if app.user %}
                        <a href="{{path('app_profil')}}" class="bouton textBlack backgroundWhite">{{app.user.pseudo}} <i class="fa-solid fa-user"></i></a>
                        <a href="{{ path('app_logout') }}" class="bouton textBlack backgroundWhite">Me déconnecter</a>
                    {% else %}
                        <a href="{{ path('app_register') }}" class="bouton textBlack backgroundWhite">S'inscrire</a>
                        <a href="{{ path('app_login')}}" class="bouton textBlack backgroundWhite">Se connecter</a>
                    {% endif %}
            </nav>
        </header>

        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
        
        
        {% block body %}
        {% endblock %}

        <footer>
            <i class="fa-brands fa-snapchat"></i>
            <i class="fa-brands fa-instagram"></i>
            <i class="fa-brands fa-youtube"></i>
            <hr>
            <a href="{{path('legal_privacy_policy')}}" class="textBlack linkBtn">Politique de confidentialité</a>
            <a href="{{path('legal_mentions')}}" class="textBlack linkBtn">Mentions légales</a>
            <a href="{{path('contact_form')}}" class="textBlack linkBtn">Contact</a>
            <a href="" class="textBlack linkBtn">FAQ</a>
            <a href="{{path('legal_cgv')}}" class="textBlack linkBtn">Condition générale de vente</a>
            <a href="{{path('legal_cookies')}}" class="textBlack linkBtn">Cookie</a>
        </footer>
    </body>
</html>
