<!DOCTYPE html>
<html lang="fr">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Home{% endblock %}</title>
        {% block stylesheets %}
            <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.7.2/css/all.min.css" integrity="sha512-Evv84Mr4kqVGRNSgIGL/F/aIDqQb7xQ2vcrdIwxfjThSH8CSR7PBEakCr51Ck+w+/U6swU2Im1vVX0SVk9ABhg==" crossorigin="anonymous" referrerpolicy="no-referrer">
            <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-4Q6Gf2aSP4eDXB8Miphtr37CMZZQ5oXLH2yaXMJ2w8e2ZtHTl7GptT4jmndRuHDT" crossorigin="anonymous">
            <link href="https://fonts.googleapis.com/css2?family=Merriweather&family=Playfair+Display&display=swap" rel="stylesheet">
            <link href="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.9/main.min.css" rel="stylesheet" />
        {% endblock %}
        {% block meta %}
            {% block og_type %}{% endblock %}
            {% block og_title %}{% endblock %}
            {% block og_description %}{% endblock %}
            {% block og_image %}{% endblock %}
            {% block description %}{% endblock %}
            {% block keywords %}{% endblock %}
            {% block author %}<meta name="author" content="Oarlina">{% endblock %}
            {% block icon %} <link rel="icon" href="{{asset('img/avatar_base.png')}}" type="image/x-icon"> {% endblock %}
        {% endblock %}
        {% block javascripts %}
            {% block importmap %}{{ importmap('app') | raw }}{% endblock %}
            <script src="https://cdn.jsdelivr.net/npm/fullcalendar@6.1.11/index.global.min.js"></script>
            <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.6/dist/js/bootstrap.bundle.min.js" integrity="sha384-j1CDi7MgGQ12Z7Qab0qlWQ/Qqz24Gc6BM0thvEMVjHnfYGF0rmFCozFSxQBxwHKO" crossorigin="anonymous"></script>
            <script>
                document.addEventListener("tac.loaded", () => {
                    if (typeof grecaptcha !== "undefined") {
                        grecaptcha.ready(() => {
                            // Ici tu peux appeler grecaptcha.execute() ou ton traitement custom
                        });
                    }
                });
            </script>
            <script src="/tarteaucitron/tarteaucitron.js"></script>
            <script>
                tarteaucitron.init({
                    "privacyUrl": "", /* URL de la politique de confidentialité */
                    "bodyPosition": "bottom", /* ou "top" pour placer l'élément en premier pour l'accessibilité */

                    "hashtag": "#tarteaucitron", /* Ouvre le panneau avec ce hashtag */
                    "cookieName": "tarteaucitron", /* Nom du cookie */

                    "orientation": "middle", /* Position de la bannière (top - bottom - middle - popup) */

                    "groupServices": false, /* Grouper les services par catégorie */
                    "showDetailsOnClick": true, /* Cliquez pour développer la description */
                    "serviceDefaultState": "wait", /* État par défaut (true - wait - false) */

                    "showAlertSmall": false, /* Afficher la petite bannière en bas à droite */
                    "cookieslist": false, /* Afficher la liste des cookies */
                    
                    "showIcon": true, /* Afficher l'icône de gestion des cookies */
                    // "iconSrc": "", /* Optionnel : URL ou image encodée en base64 */
                    "iconPosition": "BottomRight", /* Position de l’icône : BottomRight, BottomLeft, TopRight ou TopLeft */

                    "adblocker": false, /* Afficher un avertissement si un bloqueur de pub est détecté */

                    "DenyAllCta" : true, /* Afficher le bouton "Tout refuser" */
                    "AcceptAllCta" : true, /* Afficher le bouton "Tout accepter" lorsque highPrivacy est activé */
                    "highPrivacy": true, /* FORTEMENT RECOMMANDÉ : désactiver le consentement automatique */
                    "alwaysNeedConsent": false, /* Demander le consentement pour les services en "Privacy by design" */
                    
                    "handleBrowserDNTRequest": false, /* Si Do Not Track == 1, tout refuser */

                    "removeCredit": false, /* Supprimer le lien de crédit */
                    "moreInfoLink": true, /* Afficher le lien "En savoir plus" */
                    "useExternalCss": false, /* Si false, le fichier tarteaucitron.css sera chargé */
                    "useExternalJs": false, /* Si false, le fichier tarteaucitron.services.js sera chargé */

                    // "cookieDomain": ".mon-domaine-multisite.fr", /* Cookie partagé entre les sous-domaines */

                    "readmoreLink": "", /* Modifier le lien "en savoir plus" par défaut pointant vers tarteaucitron.io */
                    
                    "mandatory": true, /* Afficher un message concernant les cookies obligatoires */
                    "mandatoryCta": true, /* Afficher le bouton d’acceptation désactivé quand "mandatory" est activé */
                    
                    // "customCloserId": "", /* Optionnel a11y : ID d’un élément personnalisé pour ouvrir le panneau */

                    "googleConsentMode": true, /* Activer Google Consent Mode v2 pour les publicités Google et GA4 */
                    "bingConsentMode": true, /* Activer Bing Consent Mode pour Clarity et Bing Ads */
                    "softConsentMode": false, /* Mode de consentement souple (le consentement est requis pour charger les services) */

                    "dataLayer": false, /* Envoyer un événement à dataLayer avec l’état des services */
                    "serverSide": false, /* Côté serveur uniquement, les balises ne sont pas chargées côté client */
                    
                    "partnersList": false /* Affiche le nombre de partenaires dans le popup et la bannière du milieu */
                });
            </script>
        {% endblock %}
    </head>
    <body>
        <header class="width100">
            <nav class="backgroundBrown width100 flex alignCenter">
                {# je met le / devant pour forcer l'image à être récupérer à la racine car sinon il cherche juste dans le doosier avant #}
                {# si je suis dans la page compte/2 il cherchera l'image dans compte/img/plumeida.png #}
                <a href="" id="burger-menu"><i class="fa-solid fa-bars"></i></a>
                <a href="/.." class="logoSite"> <img src="/img/plumedia.png" alt="Plumedia logo" class="logoSite"></a>
                <a href="{{path('app_category')}}" class="bouton boutonWhite">Catégories</a>
                <a href="{{path('calendar_year')}}" class="bouton boutonWhite" data-turbo="false">Calendrier</a>
                <a href="{{path('populars')}}" class="bouton boutonWhite">Populaires</a>
                {# si l'utilisateur est connecter alors je permet directement la déconnexion et l'accès à son compte  #}
                    {% if app.user %}
                        <a href=""><i class="fa-solid fa-bell"></i><mark>5</mark></a>
                        <a href="{{path('app_profil')}}" class="bouton boutonWhite">{{app.user.pseudo}} <i class="fa-solid fa-user"></i></a>
                        <a href="{{ path('app_logout') }}" class="bouton boutonWhite">Me déconnecter</a>
                    {% else %}
                        <a href="{{ path('app_register') }}" class="bouton boutonWhite">S'inscrire</a>
                        <a href="{{ path('app_login')}}" class="bouton boutonWhite">Se connecter</a>
                    {% endif %}
            </nav>
        </header>
        {% for label, messages in app.flashes %}
            {% for message in messages %}
                <div class="alert alert-{{ label }} textWhite width100 textCenter">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
        <div class="wrapper">
            {% block body %}
            {% endblock %}
        </div>
        {# je creer un bouton pour remonter toute la page #}
        <button id="scrollToTopBtn"> &#8593;</button> 
        <footer>
            <hr>
            <section class="footer-content flex">
                <div class="footer-column textCenter">
                    <h3>About Us</h3>
                    <p>Plumédia, c’est le repaire des amoureux des mots. Ici, on lit, on écrit, on rêve — et parfois tout en même temps ! Rejoins une communauté créative où chaque histoire compte. ✨📚</p>
                </div>
                <div class="footer-column textCenter flex flexDirectionCollumn">
                    <h3>Liens utiles</h3>
                    <a href="/.." class="bouton boutonWhite">Home</a>
                    <a href="{{path('app_category')}}" class="bouton boutonWhite">Catégories</a>
                    <a href="{{path('contact_form')}}" class="bouton boutonWhite">Contact</a>
                </div>
                <div class="footer-column textCenter">
                    <h3>Contact</h3>
                    <p> <i class="fa fa-envelope"></i> marquantrachel@gmail.com</p>
                </div>
            </section>
            <div class="footer-menu textCenter">
                <a href="{{path('legal_mentions')}}" class=" bouton boutonWhite">Mentions légales</a>
                <a href="{{path('legal_cgv')}}" class=" bouton boutonWhite">Condition générale de vente</a>
                <a href="{{path('legal_privacy_policy')}}" class=" bouton boutonWhite">Politique de confidentialité</a>
                <a href="{{path('legal_cookies')}}" class=" bouton boutonWhite">Cookie</a>
                <a href="{{path('contact_form')}}" class=" bouton boutonWhite">Contact</a>
                <a href="" class=" bouton boutonWhite">FAQ</a>
            </div>
            <div class="footer-copyright textCenter flex ">
                <div class="footer-column textCenter">
                    <p> &copy; Plumédia </p>
                </div>
                <div class="footer-column textCenter">
                    <p> Développer par Rachel Marquant</p>
                </div>
            </div>
        </footer>
    </body>
</html>
